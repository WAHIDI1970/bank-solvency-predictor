👉   pipeline = Pipeline([
    ('scaler', StandardScaler()),
    ('smote', SMOTE(random_state=42)),
    ('clf', LogisticRegression(random_state=42))
    ])


✅  pipeline permetent de tester plusieurs parametres pour trouver le meilleur model avec K-fold Cross Validation:
  grid = GridSearchCV(pipeline, param_grid, cv=5, scoring='f1')
  grid.fit(X_train, y_train)



📌📌📌📌 model.fit(Xe_scaled)<-> model.predict(Xt_scaled)📌📌📌📌📌



👉 👉 👉   to create an ml app with streamlit:
            - creat a repostory in Github .
            - you need to creat a nootebook , download your model and your scaler as pkl file .
            - create your app.py it is streamlit app code .
            - add requirements.txt file (file with needed labraries)
            - add Data.csv
            - your repostory is ready , you can just copy the REPO link in streamlit.Cloud and deploy your app .